# Copy to backend/.env on the server and fill in values

ANTHROPIC_API_KEY=sk-ant-your-key-here
CLAUDE_MODEL=claude-sonnet-4-6

# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-string

ACCESS_TOKEN_EXPIRE_MINUTES=10080

# SQLite stored in the persistent Docker volume — do not change this path
DATABASE_URL=sqlite:////data/theater.db

# Replace with your server's IP (e.g. http://65.21.44.12)
FRONTEND_URL=http://YOUR_SERVER_IP
